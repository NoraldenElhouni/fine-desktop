import React from "react";
import { createRoot } from "react-dom/client";

const root = createRoot(document.body);
root.render(
  <h2 className="text-2xl font-bold text-gray-100">Hello from React!</h2>,
);
